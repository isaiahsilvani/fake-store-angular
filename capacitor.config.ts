import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-test-isaiah',
  webDir: 'dist/angular-test-isaiah/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
